/**
 * Interface to be used for Collection Data
 */
export interface ICollection {
  id: string;
  name: string;
  detail: string;
}

/**
 * Interface to be used for CollectionType from API
 */
export interface ICollectionType {
  id: string;
  name: string;
}
